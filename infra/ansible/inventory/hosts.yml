all:
  hosts:
    nucbox:
      # Résolu via l'alias SSH `nucbox` déjà configuré (~/.ssh/config,
      # clé dédiée `id_nucbox`) — pas besoin de dupliquer IP/utilisateur/clé
      # ici, Ansible réutilise la config SSH existante.
      ansible_host: nucbox
